-- Custom SQL migration file, put your code below! --

-- Create agent_threads_thread table
CREATE TABLE IF NOT EXISTS "agent_threads_thread" (
  "id" bigint PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  "created_at" timestamp with time zone NOT NULL DEFAULT now(),
  "updated_at" timestamp with time zone NOT NULL DEFAULT now(),
  "mailbox_id" bigint NOT NULL,
  "slack_channel" text NOT NULL,
  "thread_ts" text NOT NULL
);

-- Create agent_messages_message table
CREATE TABLE IF NOT EXISTS "agent_messages_message" (
  "id" bigint PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  "created_at" timestamp with time zone NOT NULL DEFAULT now(),
  "updated_at" timestamp with time zone NOT NULL DEFAULT now(),
  "agent_thread_id" bigint NOT NULL,
  "role" text NOT NULL,
  "content" text NOT NULL,
  "tool_name" text,
  "metadata" jsonb
);

-- Create indexes
CREATE INDEX "agent_threads_mailbox_id_idx" ON "agent_threads_thread" ("mailbox_id");
CREATE INDEX "agent_threads_slack_channel_thread_ts_idx" ON "agent_threads_thread" ("slack_channel", "thread_ts");
CREATE INDEX "agent_messages_agent_thread_id_idx" ON "agent_messages_message" ("agent_thread_id");
CREATE INDEX "agent_messages_role_idx" ON "agent_messages_message" ("role");

-- Add foreign key constraints
ALTER TABLE "agent_threads_thread" ADD CONSTRAINT "agent_threads_mailbox_id_fkey" 
  FOREIGN KEY ("mailbox_id") REFERENCES "mailboxes_mailbox" ("id") ON DELETE CASCADE;

ALTER TABLE "agent_messages_message" ADD CONSTRAINT "agent_messages_agent_thread_id_fkey" 
  FOREIGN KEY ("agent_thread_id") REFERENCES "agent_threads_thread" ("id") ON DELETE CASCADE;
